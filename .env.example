# Shared token between the Dota GSI .cfg file and this server.
# Must match the token defined in the .cfg exactly.
GSI_AUTH_TOKEN=change_this_token

# Ingestion server address and port.
SERVER_HOST=127.0.0.1
SERVER_PORT=4000

# Local Ollama model used by the coach brain.
OLLAMA_MODEL=qwen2.5:14b